# database properties
# remove '#' from last 4 lines
#replace *** with database name
#enter username and password next to = sign without spaces.
app.jdbc.driverClassName=org.postgresql.Driver
app.jdbc.url=jdbc:postgresql://localhost:5432/protocol
app.jdbc.username=protocoluser
app.jdbc.password=protocolpassword

